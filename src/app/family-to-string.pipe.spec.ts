import { FamilyToStringPipe } from './family-to-string.pipe';

describe('FamilyToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FamilyToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
