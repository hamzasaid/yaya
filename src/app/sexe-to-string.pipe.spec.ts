import { SexeToStringPipe } from './sexe-to-string.pipe';

describe('SexeToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SexeToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
